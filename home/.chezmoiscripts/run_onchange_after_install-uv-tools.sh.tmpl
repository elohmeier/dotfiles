#!/bin/sh
# pyproject: {{ include "../pyproject.toml" | sha256sum }} {{ include "../uv.lock" | sha256sum }}

command -v uv >/dev/null
uv tool install --force --upgrade "{{ .chezmoi.sourceDir }}/.."
# required by jupytext.nvim plugin to convert .ipynb files
uv tool install --upgrade jupytext
