#!/bin/sh
# Rebuild bat cache when themes change
# Hash: {{ include (printf "dot_config/bat/themes/%s.tmTheme" .theme) | sha256sum }}

command -v bat >/dev/null && bat cache --build >/dev/null 2>&1 || true
