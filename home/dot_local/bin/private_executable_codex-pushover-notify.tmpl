{{- $curl := lookPath "curl" -}}
#!{{ lookPath "sh" }}
{{ $curl }} -fsS \
    --form-string 'token={{ .pushoverApiToken }}' \
    --form-string 'user={{ .pushoverUserKey }}' \
    --form-string 'title=Codex finished' \
    --form-string "message=Finished work in ${PWD##*/}." \
    https://api.pushover.net/1/messages.json >/dev/null
